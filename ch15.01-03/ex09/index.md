## 調べたフレームワーク
React

## XSS対策
- **エスケープ**  
  JSXでは、入力したテキストを自動的にHTMLエスケープしてくれるため、基本的なXSSを防いでくれる

## XSSの危険
- **`dangerouslySetInnerHTML`を使用する**  
  文字列をそのまま挿入するため、悪意あるコードが実行されてしまう可能性がある
  
- **属性値やURLなどにコードとして解釈されるデータを渡す**  
  入力された値がコードとして実行されてしまう危険がある